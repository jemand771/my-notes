source 'https://rubygems.org'
gem "jekyll-theme-leaf"
# okay, weird thing (I don't know ruby):
# without the Gemfile, jekyll build works just fine
# however, if I add jekyll-theme-leaf, it says that kramdown-parser-gfm is missing
# so here I am adding it manually
gem "kramdown-parser-gfm"
